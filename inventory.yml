echo "# ansible_phpadmin_database" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/tetemma/ansible_phpadmin_database.git
git push -u origin main
